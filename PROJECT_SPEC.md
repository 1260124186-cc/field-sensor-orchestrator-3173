# Field Sensor Orchestrator 项目规格

## 项目目标
为野外生态监测团队提供一个本地 CLI，用于登记传感器、编排采集任务、执行采集周期、确认校准、管理告警以及导出观测摘要。系统强调设备生命周期、任务租约、跨站点状态隔离和可追踪的错误传播。

## 用户角色
- 现场技术员：登记与校准传感器、查看设备状态。
- 观测协调员：创建采集任务、运行周期并处理告警。
- 数据管理员：导出指定站点的观测摘要并检查审计事件。

## 核心实体
传感器、监测站点、采集任务、租约、观测读数、校准记录、告警和审计事件。

## 业务流程
1. `register-sensor`：校验站点与设备描述，登记传感器并生成审计事件。
2. `schedule-cycle`：选择活动传感器，建立带截止时间的采集任务和设备租约。
3. `run-cycle`：领取任务、并发采集读数、传播取消与设备错误、完成任务并释放租约。
4. `calibrate-sensor`：应用校准读数，更新设备状态并记录校准事件。
5. `ack-alert`：确认告警并保持站点级状态隔离。
6. `export-summary`：聚合站点观测数据，输出健康度与任务摘要。

## 状态与规则
传感器在 active、calibrating、degraded、retired 间流转；任务在 queued、leased、running、completed、failed、cancelled 间流转。每个活动任务必须拥有租约，结束路径必须释放租约。取消信号必须贯穿调度、执行与采集层，站点数据不可跨请求污染。

## 接口与验证
入口为 `go run ./cmd/sensorctl <action>`。运行清单逐个执行登记、排程、采集、校准、告警确认和摘要导出流程；标准构建与测试命令为 `go build ./...` 与 `go test ./...`。
